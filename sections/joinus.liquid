<!-- <div class="newsletter {% if newsletter == 'center' %}center{% endif %}">
  <p class="message"></p>
  
  {% form 'customer' %}
    {% if form.posted_successfully? %}
      <p><em>{{ 'general.newsletter_form.success_text' | t }}</em></p>
    {% elsif form.errors %}
      {% for field in form.errors %}
        <p><em>{{ field }} - {{ form.errors.messages[field] }}</em></p>
      {% endfor %}
    {% endif %}

    {% if template contains "password" %}
     <input type="hidden" name="contact[tags]" value="prospect,password page"/>
    {% else %}
      <input type="hidden" name="contact[tags]" value="newsletter">
    {% endif %}        
    
    {% if settings.display_first_name %}
      <input class="newsletter-half first-name" type="text" name="contact[first_name]" placeholder="{{ 'general.newsletter_form.subscriber_first_name' | t }}" />
    {% endif %}
    {% if settings.display_last_name %}
      <input class="newsletter-half last-name" type="text" name="contact[last_name]" placeholder="{{ 'general.newsletter_form.subscriber_last_name' | t }}" />
    {% endif %}
     
    <input type="hidden" name="challenge" value="false">  
    <input type="email" class="contact_email" name="contact[email]" required placeholder="{{settings.Email_placeholder}}" autocorrect="off" autocapitalize="off" />
    <input type='submit' name="commit" class="action_button sign_up" value="{{settings.button_text}}" />
  {% endform %}
</div> -->

<div class="newspoup">
<form action="{{ section.settings.action_url }}" method="post" name="mc-embedded-subscribe-form" class="contact-form desk_news" target="_blank">
      
      <input type="email" value="" name="EMAIL" class="contact_email" placeholder="{{ section.settings.email_placeholder }}">
      <input type="submit" value="{{ section.settings.button_text }}" class="action_button sign_up" name="subscribe">
    </form>
</div>


{% schema %}
  {
    "name": "Join Us",
    "settings": [
     {
       "type":"text",
        "id" :"action_url",
        "label":"MailChimp Form Action URL"
     },
     {
        "type":"image_picker",
          "id":"images",
          "label":"background image"
     },
     {
       "type":"text",
        "id" :"email_placeholder",
      "label":"Enter Email Placeholder"
     },
     {
       "type":"text",
        "id" :"button_text",
      "label":"Button text"
     }
  ],
  "presets": [{
    "name": "Join Us form",
    "category": "Social media"
  }]}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
